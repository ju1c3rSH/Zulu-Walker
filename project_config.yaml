platform: maixcam2

cameras:
  - camera_id: "cam0"
    source: 0
    width: 320
    height: 240
    fps: 60
    camera_stream_queue_size: 3

# Demo phase — no UART device connected; MaixCam2Uart handles absence gracefully
# uart_defaults:
#   port: "/dev/ttyS4"
#   baudrate: 921600

ai:
  models:
    - nick_name: "yolo"
      model: "/root/models/yolo11n.mud"
      model_type: "yolo"
    - nick_name: "hand"
      model: "/root/models/hand_landmarks.mud"
      model_type: "hand_landmarks"
  active: "yolo"
