pipelines:
  - pipeline_id: "cam_main"
    camera_id: "main"
    tasks:
      - name: "ai_inference"
        type: "AIInferenceProcessor"
        enabled: true
      - name: "line_follow"
        type: "LineFollowProcessor"
        enabled: false
